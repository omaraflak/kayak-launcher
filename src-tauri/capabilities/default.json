{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the launcher window. The Kayak window is deliberately absent: it renders a page served from the container, which must not be able to reach the IPC bridge.",
  "windows": ["launcher"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "opener:default",
    "opener:allow-open-url",
    "process:allow-restart",
    "updater:default"
  ]
}
